# dbt, DuckDB, and Dagster

A local data project built around dbt, with Python ingestion, Dagster orchestration,
Parquet storage, and DuckDB analytics.

Planning is underway. The pipeline is not runnable yet.
